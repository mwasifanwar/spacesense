data:
  image_size: [512, 512]
  telescope_fov: 2.0
  min_debris_size: 0.1

computer_vision:
  detection_threshold: 0.7
  max_tracking_points: 1000
  model_path: "models/debris_detector.pth"

orbital_mechanics:
  time_step: 60
  prediction_horizon: 86400
  collision_threshold: 100.0

reinforcement_learning:
  learning_rate: 0.0003
  gamma: 0.99
  buffer_size: 100000
  batch_size: 64

api:
  host: "0.0.0.0"
  port: 8000